Процедура ОбработкаПроведения(Отказ, РежимПроведения)

	Движения.ЦеныНапитков.Записывать = Истина;

	Для Каждого Строка Из Цены Цикл

		Запись = Движения.ЦеныНапитков.Добавить();
		Запись.Период  = Дата;
		Запись.Автомат = Строка.Автомат;
		Запись.Напиток = Строка.Напиток;
		Запись.Цена    = Строка.Цена;

	КонецЦикла;

КонецПроцедуры